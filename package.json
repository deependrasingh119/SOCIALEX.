{
  "name": "my-project",
  "version": "1.0.0",
  "description": "Full-stack social media application",
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "npm --prefix client install && npm --prefix client run build",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm --prefix client install && npm --prefix client run build"
  },
  "dependencies": {
    "mongoose": "^8.18.2"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
